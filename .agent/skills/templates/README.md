// Đây là nơi bạn đặt các file mẫu (templates)
// Ví dụ:
// - component.tsx (Mẫu chuẩn cho React Component)
// - api-service.ts (Mẫu chuẩn cho gọi API)
// - page.tsx (Mẫu chuẩn cho Page)

// Khi AI cần tạo file mới, nó sẽ đọc các file trong này để copy structure.
